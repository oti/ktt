-
  const splited = image.split("-")
  const alt = `${splited[0]}年${Number(splited[1])}月${Number(splited[2])}日に食べたTKG写真`
li(id=`photo-${image}`)
  a.thumb(href=`./image/photo/${image}.jpg`)
    img(
      src=`./image/photo/thumb_${image}.jpg`,
      alt=`${alt}のサムネール`,
      loading="lazy",
      width="168",
      height="168"
    )
